# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env — never commit keys
.env
.env.local
.env.*.local

# Vercel project link
.vercel

# Supabase CLI local state
supabase/.temp
supabase/.branches

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android — the TWA shell. Build outputs and, critically, the signing key:
# losing the keystore means never being able to update the app again, and
# committing it means anyone with repo access can publish as you.
android/build/
android/app/build/
android/.gradle/
*.keystore
*.jks

# Bubblewrap generates the whole Android project from twa-manifest.json, so the
# manifest is the source and everything else is output. Keeping the generated
# Gradle project out avoids a large diff on every rebuild.
android/*
!android/twa-manifest.json
